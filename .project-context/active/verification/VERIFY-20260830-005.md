---
id: VERIFY-20260830-005
type: verification-evidence
status: passed
title: RnD Workbench 0.8.0 build 6 release verification
created_at: '2026-08-30T17:49:40+03:00'
updated_at: '2026-08-30T18:24:02+03:00'
target_id: DECISION-20260830-011
target_type: record
recorded_by: agent
modules:
  - backend
  - desktop
files:
  - src/voice_assistant/backends.py
  - src/voice_assistant/orchestrator.py
  - src/voice_assistant/store.py
  - src/voice_assistant/ui_backend.py
  - macos/VoiceAssistantApp.swift
  - macos/Info.plist
  - README.md
tags:
  - verification
  - release
  - external-llm
  - meetings
  - deletion
  - codesign
checks:
  - command: .venv/bin/python -m pytest -q
    status: passed
  - command: .venv/bin/python -m compileall -q src
    status: passed
  - command: swiftc typecheck macos/VoiceAssistantApp.swift
    status: passed
  - command: endpoint canonicalization self-test
    status: passed
  - command: scripts/verify_external_llm.py
    status: passed
  - command: bundled OmniVoice benchmark
    status: passed
  - command: codesign --verify --deep --strict RnD Workbench.app
    status: passed
  - command: live SQLite schema and integrity queries
    status: passed
retention: keep
---

# RnD Workbench 0.8.0 build 6 release verification

## Results

- Python suite: **138 passed**.
- SwiftUI typecheck: passed.
- Endpoint canonicalization self-test: **18/18 passed**.
- Local OpenAI-compatible SSE mock: passed; endpoint, model, streaming roles
  and response contract matched.
- Audio normalization contract: actual `afconvert` output was mono, 16 kHz,
  signed 16-bit PCM WAVE.
- Bundled OmniVoice Q8 / Metal: first audio 0.425 s; synthesis 2.156 s for
  8.610 s of audio; RTF 0.250; peak 0.873138; clipped samples 0; cancellation
  0.229 s; recovery first audio 0.928 s.
- Live SQLite: schema v4, `integrity_check = ok`, foreign-key violations 0,
  source chunks 76 and FTS rows 76.
- App launch smoke: native process, bundled Python backend and loopback
  OmniVoice server were all running from the 0.8.0 bundle.
- Final strict deep signature verification: `valid on disk` and `satisfies its
  Designated Requirement` after the last rebuild/sign step.

## Visual evidence

The supplied 1060×720 application screenshot showed long Russian titles and
actions competing for horizontal space. The Attention row now gives the title
two lines and layout priority, places the action in a bottom trailing row and
keeps the action label at its intrinsic width. The top bar uses an adaptive
compact label.

Five clean screenshots were subsequently captured from the running build 6
window: local and external variants of «Сегодня», «Встречи», external-model
settings and compact voice mode. They were embedded into a newly rebuilt
standalone HTML overview. The report explicitly distinguishes the portable
local-first core from the currently verified macOS delivery and documents
local, corporate OpenAI-compatible and external model routes without claiming
unreleased Linux or Windows packages.

The HTML overview was rendered through macOS Quick Look at 1800 px and
inspected successfully. Static checks found six embedded image instances with
non-empty alt text, six valid local anchors, no placeholders, scripts, remote
URLs or external assets.

## Residual limitations

- The app is ad-hoc signed. A future rebuild changes its cdhash and can affect
  access to Keychain items created by an earlier development build. Production
  distribution needs a stable Developer ID identity and notarization.
- Full keyboard, VoiceOver, physical-room barge-in and human listening audits
  remain manual release checks.
