---
id: VERIFY-20260830-004
type: verification-evidence
status: passed
title: RnD Workbench 0.7.0 build 5 release verification
created_at: '2026-08-30T16:53:00+03:00'
updated_at: '2026-08-30T16:55:00+03:00'
target_id: DECISION-20260830-009
target_type: record
recorded_by: agent
modules:
  - backend
  - desktop
files:
  - src/voice_assistant/app.py
  - src/voice_assistant/backends.py
  - src/voice_assistant/store.py
  - src/voice_assistant/ui_backend.py
  - macos/VoiceAssistantApp.swift
  - macos/Info.plist
tags:
  - verification
  - release
  - tts
  - sqlite
  - codesign
checks:
  - command: PYTHONPATH=src .venv/bin/python -m pytest
    status: passed
  - command: .venv/bin/python -m compileall -q src
    status: passed
  - command: swiftc typecheck macos/VoiceAssistantApp.swift
    status: passed
  - command: codesign --verify --deep --strict RnD Workbench.app
    status: passed
  - command: live SQLite schema and integrity queries
    status: passed
  - command: bundled OmniVoice benchmark and Whisper loopback
    status: passed
  - command: npm run context:lint
    status: passed
  - command: npm run context:index
    status: passed
  - command: npm run context:doctor
    status: passed-with-warnings
retention: keep
---

# VERIFY-20260830-004: RnD Workbench 0.7.0 build 5 release verification

## Target

- Release: RnD Workbench 0.7.0 (build 5).
- Decisions covered: continuous OmniVoice playback, explicit stream failures,
  nonfatal speech recovery, schema-v4 artifact provenance and the shared native
  TTS state.

## Checks Run

- `PYTHONPATH=src .venv/bin/python -m pytest` — passed, **90 tests**.
- `.venv/bin/python -m compileall -q src` — passed.
- Swift typecheck for `macos/VoiceAssistantApp.swift` — passed.
- `codesign --verify --deep --strict` for the release `.app` — passed.
- Live SQLite validation — passed: schema version 4, `integrity_check = ok`,
  foreign-key violations 0, `source_chunks` 65, `source_chunks_fts` 65.
  `artifact_relations` exists and is active; 0 rows is the expected empty state
  for current live data.
- TTS recovery contract — passed: an isolated speech failure preserves the full
  assistant text and leaves the task `done`; the backend emits `speech_error`
  and `speech_recovered`, while `assistant_end` reports `spoken` and `tts_error`.
- `retry_speech` — passed: it replays the last assistant text without another
  LLM call and without inserting a task or message into SQLite.
- Shared native state — passed by automated/backend regression and Swift
  typecheck: full and compact interfaces use `rnd.speakReplies` and contain
  their recoverable TTS banners.
- Bundled OmniVoice benchmark — passed: first audio 0.472 s; synthesis 1.939 s
  for 8.760 s audio; RTF 0.221; peak 0.884094; clipped samples 0; cancellation
  0.229 s.
- Whisper loopback — passed: transcription exactly matched the benchmark source
  text.
- `npm run context:lint` — `status: OK`, errors 0, warnings 0.
- `npm run context:index` — `status: OK`, records 16, symbols 28, endpoints 0,
  file-reference issues 0, duplicate-record issues 0.
- `npm run context:doctor` — completed with `status: WARN`: 15 checks `ok`,
  3 `warn`, 0 `fail`. Warnings are limited to the SQLite index not being ignored
  by git, draft run summaries not being ignored by git, and no ready/open
  backlog items.

## Checks Skipped

- Current screenshot-based UX audit — skipped because the Mac was locked and
  automatic unlock was unavailable.
- Visual and accessibility inspection of the full/compact TTS banners — skipped
  for the same reason; compilation is not visual verification.
- IA simplification and additional graphics redesign — not claimed as verified
  or complete until the unlocked application can be captured and inspected.

## Known Product Limitation

Artifact version listing, restore, provenance relations and Quick Actions are
ready in the backend. Their native SwiftUI controls remain pending.

## Residual Risk

The automated release checks validate code paths, persistence, signed packaging
and synthetic audio quality. They do not replace a human listening test on the
target speakers, a physical-room barge-in trial, or an unlocked screenshot,
keyboard and VoiceOver audit of build 5.
