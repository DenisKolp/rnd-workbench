# Optional Windows voice build environment. Runtime remains capability-gated:
# model weights and the OmniVoice-Fast native server are intentionally external.
-r requirements-build.txt
faster-whisper==1.2.0
requests==2.34.2
